spring:
  datasource:
    url: jdbc:mysql://localhost:3306/askgpt
    username: root
    password: 12345
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      javax:
       persistence:
        validation:
         mode: none
    show-sql: true
  data:
    web:
      pageable:
        default-page-size: 10
        max-page-size: 100